// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "picsum.photos",
      "source.unsplash.com",
      "plus.unsplash.com",
      "randomuser.me",
      "fastly.picsum.photos",
    ],
  },
};

export default nextConfig;
